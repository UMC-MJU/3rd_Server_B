spring:
  datasource:
    url: jdbc:mysql://localhost:3306/board_practice?serverTimezone=Asia/Seoul
    username: root
    password: "004637"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: trace
